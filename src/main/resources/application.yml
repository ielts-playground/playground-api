server:
  port: 8080
  security:
    cors:
      headers:
        - "*"
      methods:
        - GET
        - POST
        - PUT
        - DELETE
        - OPTIONS
      origins:
        - http://localhost:3000
    authentication:
      whitelist:
        - /public/**
        - /test/**
        - /swagger-ui/**
        - /api/v2/api-docs
      private:
        path: /private/**
        header-key: X-Api-Key
        secret-keys:
          - value: this-is-a-private-key
            name: Basic Client
      jwt:
        path: /api/**
        validity-in-seconds: 3600
        secret-key: this-is-a-jwt-secret-key

api:
  posts:
    generator:
      lorem-ipsum:
        url: https://jaspervdj.be/lorem-markdownum/markdown.txt

logging:
  file:
    name: logs/demo.log
  level:
    root: INFO
    org.ielts.playground: DEBUG

spring:
  cache:
    type: redis
  redis:
    host: localhost
    port: 6379
    password:
    ttl:
      default-in-seconds: 3600
      posts-view: 7200
      posts-search: 300
      test-audio-get: 7200
  jpa:
    open-in-view: false
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5InnoDBDialect
        # order_inserts: true
        jpa:
          batch_size: 10
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://54.251.23.202:33060/playground?useUnicode=true&characterEncoding=UTF-8
    username: root
    password: 123
  liquibase:
    change-log: classpath:db/db.changelog-master.xml
